from django.apps import AppConfig

class FindcountryConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.findcountry'  # Assure-toi que c'est bien 'apps.findcountry'
